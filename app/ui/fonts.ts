import { Inter, Poppins, Lusitana } from 'next/font/google';
export const poppins = Poppins({ subsets: ['latin-ext'], weight: '200'});
export const lusitana = Lusitana({ subsets: ['latin'], weight:'400'});